# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Tăng tài nguyên để xử lý các bộ lọc nặng và ngăn chặn lỗi 500
  minInstances: 2
  maxInstances: 10
  concurrency: 80
  cpu: 1
  memoryMiB: 1024
