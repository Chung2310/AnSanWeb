# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Tăng số lượng instance và cấu hình phần cứng để tránh lỗi 500 (Timeout)
  minInstances: 2
  maxInstances: 10
  concurrency: 80
  cpu: 1
  memoryMiB: 1024
